# Manual

Trusted first-party notes live here before or alongside normalization.

Recommended flow:

1. Save the original text note or transcript under `raw/`
2. Normalize into canonical `data/` files
3. Preserve the raw source path in `source_ref` and `source_refs`
