# Exports

Machine-facing exports are derived from `data/` and can be regenerated without changing the source-of-truth text structure.

Current generated files:

- `catalog.json`: joined place and opinion summary for downstream search
- `places.ndjson`: one place document per line
- `opinions.ndjson`: one opinion document per line
