# Plans

Store reusable routes and local-life plans here.

Recommended files:

- `data/plans/<plan_id>.json` for machine-queryable itinerary structure
- `data/plans/<plan_id>.md` for human-readable trip guidance
